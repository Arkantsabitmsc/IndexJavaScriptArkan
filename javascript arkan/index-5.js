
let person = `    {
    Name = 'arkan',
    Let Age = 25
};

// Dot Notation
person.Name = 'budi';

//bracket notation
let selection = 'name';
person[selection] = 'adam';

console.log(person.name);
